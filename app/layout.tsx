export const metadata = {
  title: 'Vania Billiard | Kurator Meja Premium',
  description: 'Distributor dan kurator meja billiard turnamen premium.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children;
}
